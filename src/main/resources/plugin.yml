name: RandomTeleport
version: @version@
main: black.bracken.randomteleport.RandomTeleport
authors: []

commands:
  randomteleport:
    aliases: [rtp]
    permission: randomteleport.use

permissions:
  randomteleport.*:
    children:
      randomteleport.use: true
  randomteleport.use:
    default: true
